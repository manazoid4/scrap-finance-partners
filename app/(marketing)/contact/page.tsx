import type { Metadata } from "next"; import LeadForm from "@/components/lead-form";
export const metadata:Metadata={title:"Contact",description:"Start a commercial finance conversation with Scrap Finance Partners."};
export default async function Contact({searchParams}:{searchParams:Promise<{intent?:string}>}){const {intent="health-check"}=await searchParams; const yard=intent==="yardledger"; return <><section className="border-b border-hairline bg-grid"><div className="mx-auto w-full max-w-5xl px-4 py-16 sm:px-6 sm:py-20"><p className="text-xs font-semibold uppercase tracking-[.16em] text-copper">{yard?"YardLedger interest":"Start a conversation"}</p><h1 className="mt-4">{yard?"Tell us what you would want YardLedger to solve.":"What commercial question needs attention?"}</h1><p className="mt-6 max-w-2xl text-lg text-ink-secondary">{yard?"This is research interest, not a purchase or promise of availability.":"Give us enough context to decide whether a Health Check or a different next step makes sense."}</p></div></section><section><div className="mx-auto grid w-full max-w-5xl min-w-0 grid-cols-1 gap-10 px-4 py-16 sm:px-6 lg:grid-cols-[minmax(0,.4fr)_minmax(0,.6fr)]"><div><h2>What happens next</h2><p className="mt-4 text-ink-secondary">Your enquiry is reviewed by a person. We will use the details only to respond and will be direct if the work is not a fit.</p></div><LeadForm source="contact-page" intent={intent} submitLabel={yard?"Register interest":"Request a Health Check"}/></div></section></>}