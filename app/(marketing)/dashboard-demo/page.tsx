import Link from "next/link";

const rows=[{label:"Trading margin",status:"Review",note:"Compare apparent spread with haulage and operating involvement."},{label:"Stock confidence",status:"Check",note:"Reconcile the records used for decisions with the physical process."},{label:"Transport capacity",status:"Review",note:"Show cost and capacity alongside the deal, not after it."},{label:"Reporting rhythm",status:"Clarify",note:"Connect each measure to a decision and an owner."}];
export default function DashboardDemoPage(){return <div className="min-w-0 bg-graphite"><section className="border-b border-hairline bg-navy"><div className="mx-auto w-full max-w-6xl px-4 py-10 sm:px-6"><p className="text-xs font-semibold uppercase tracking-[.16em] text-copper">Illustrative concept · sample data only</p><h1 className="mt-3 text-white">Example commercial review view</h1><p className="mt-4 max-w-2xl text-ink-secondary">This is a visual example, not a live client dashboard and not evidence of a measured result.</p></div></section><section><div className="mx-auto w-full max-w-6xl px-4 py-12 sm:px-6"><div className="grid min-w-0 grid-cols-1 gap-4 sm:grid-cols-2">{rows.map(row=><article key={row.label} className="min-w-0 border border-hairline bg-panel p-5 sm:p-6"><div className="flex min-w-0 items-start justify-between gap-4"><h2 className="font-sans text-lg">{row.label}</h2><span className="shrink-0 border border-copper px-2 py-1 text-xs font-semibold text-copper">{row.status}</span></div><p className="mt-4 text-sm text-ink-secondary">{row.note}</p></article>)}</div><div className="mt-8 border-l-4 border-copper bg-panel p-6"><h2 className="font-sans text-xl">The point is the decision, not the dashboard.</h2><p className="mt-3 text-ink-secondary">A Health Check can establish which information matters before any reporting or software work is scoped.</p><Link href="/health-check" className="mt-5 inline-flex min-h-11 items-center font-semibold text-copper">Explore the Health Check →</Link></div></div></section></div>}