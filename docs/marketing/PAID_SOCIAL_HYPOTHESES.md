# Paid social hypotheses — draft, not approved for launch

Destination for every concept: `/health-check`. Medium: `paid_social`. Source: `instagram` or `facebook`.

## 1. Stock gap

Hypothesis: direct stock-sheet-versus-yard recognition will produce more qualified enquiries than generic finance wording.

- Static A: “THE STOCK SHEET SAYS ONE THING. THE YARD SAYS ANOTHER.” Content `stock_gap_static_a`.
- Carousel B: “3 PLACES STOCK NUMBERS LOSE THE YARD.” Content `stock_gap_carousel_b`.
- Campaign: `health_check_stock_gap_v1`.

## 2. Margin visibility

Hypothesis: margin-per-tonne pain will outperform broad “better reporting”.

- Static A: “BUSY YARD. NO CLEAR MARGIN PER TONNE.” Content `margin_static_a`.
- Carousel B: “A GOOD SALES PRICE CAN STILL HIDE A BAD MARGIN.” Content `margin_carousel_b`.
- Campaign: `health_check_margin_visibility_v1`.

## 3. Transport cost

Hypothesis: transport cost appearing after the deal will attract high-fit operators.

- Static A: “THE DEAL LOOKS FINE—UNTIL TRANSPORT LANDS.” Content `transport_static_a`.
- Reel B: “BUY PRICE. SELL PRICE. HAULAGE. WHAT IS THE REAL MARGIN?” Content `transport_reel_b`.
- Campaign: `health_check_transport_cost_v1`.

Each creative must name its ICP, disqualifier, approved claim ID, CTA and format before approval. Pause immediately for broken tracking, claim/policy risk, landing mismatch or invalid-lead spikes. Set `[MIN_TEST_DURATION]`, `[MIN_SPEND_OR_REACH]`, `[MIN_QUALIFIED_LEADS]` and `[MAX_CPQL]` before launch. Never optimise on CTR alone; do not publish or spend from this scaffold.